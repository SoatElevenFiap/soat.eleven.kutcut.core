apiVersion: v1
kind: Service
metadata:
  name: {{ include "kutcut-api.fullname" . }}
  labels:
    {{- include "kutcut-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kutcut-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
